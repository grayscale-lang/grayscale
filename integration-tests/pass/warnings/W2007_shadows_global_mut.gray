/*
 * Warning Test: W2007 - shadows a global mut variable
 * Expected: warning about variable shadowing a global
 */

mut counter i64 = 0

do main() {
    mut counter i64 = 10  // shadows the global mut
    println(counter)
}
