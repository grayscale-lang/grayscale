/*
 * Warning Test: W2007 - shadows a global inside a helper function
 * Expected: warning about variable shadowing a global
 */

const LIMIT i64 = 50

do helper() {
    mut LIMIT i64 = 25  // shadows global const in a non-main function
    println(LIMIT)
}

do main() {
    helper()
}
