/*
 * Warning Test: W3003 - array-size-mismatch
 * Expected: warning about fixed-size array not fully initialized
 */


do main() {
    // Declare array with size 5 but only provide 3 elements
    const arr [i64, 5] = {1, 2, 3}
    println(arr)
}
