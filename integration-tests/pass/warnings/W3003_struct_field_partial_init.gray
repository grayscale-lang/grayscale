/*
 * Warning Test: W3003 - struct-literal fixed-size field partially initialized
 * Expected: warning about fixed-size array field not fully initialized
 */

const Buffer struct {
    data [int, 5]
}

do main() {
    const b Buffer = Buffer{data: {1, 2}}
    println(b.data)
}
