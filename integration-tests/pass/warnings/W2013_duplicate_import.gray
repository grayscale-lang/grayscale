/*
 * Warning Test: W2013 - duplicate import of already-imported module
 * Expected: warning about importing the same module twice
 */

import "./W2013_helpers/helper"
import "./W2013_helpers/helper"

do main() {
    mut x int = helper.w2013_value()
    println(x)
}
