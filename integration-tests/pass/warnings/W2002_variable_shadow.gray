/*
 * Warning Test: W2002 - variable-shadow
 * Expected: warning about variable shadowing an outer scope variable
 */


do main() {
    mut x i64 = 10
    if true {
        mut x i64 = 20  // Shadows outer x
        println(x)
    }
    println(x)
}
