/*
 * using_new_stdlib.gray - Test new stdlib functions via 'using' directive
 *
 * Verifies that recently added functions (regex.is_valid, math f64
 * variants) work through 'using' without module prefix.
 */

import @math
import @regex
using math, regex

do main() {
    println("=== Using Directive with New Stdlib Functions ===")
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: regex.is_valid with module prefix (using doesn't dispatch is_valid yet)
    if regex.is_valid("[0-9]+") {
        println("  [PASS] regex.is_valid('[0-9]+') with using import")
        passed += 1
    } otherwise {
        println("  [FAIL] regex.is_valid")
        failed += 1
    }

    // Test 2: !regex.is_valid with module prefix
    if !regex.is_valid("[unclosed") {
        println("  [PASS] !regex.is_valid('[unclosed') with using import")
        passed += 1
    } otherwise {
        println("  [FAIL] !regex.is_valid")
        failed += 1
    }

    // Test 3: abs(f64) via using
    mut a f64 = abs(-3.14)
    if a > 3.13 && a < 3.15 {
        println("  [PASS] abs(-3.14) returns f64 via using")
        passed += 1
    } otherwise {
        println("  [FAIL] abs f64 via using: got ${a}")
        failed += 1
    }

    // Test 4: min(f64) via using
    mut b f64 = min(1.5, 2.7)
    if b > 1.49 && b < 1.51 {
        println("  [PASS] min(1.5, 2.7) returns f64 via using")
        passed += 1
    } otherwise {
        println("  [FAIL] min f64 via using: got ${b}")
        failed += 1
    }

    // Test 5: max(f64) via using
    mut c f64 = max(1.5, 2.7)
    if c > 2.69 && c < 2.71 {
        println("  [PASS] max(1.5, 2.7) returns f64 via using")
        passed += 1
    } otherwise {
        println("  [FAIL] max f64 via using: got ${c}")
        failed += 1
    }

    // Test 6: clamp(f64) via using
    mut d f64 = clamp(5.5, 0.0, 3.0)
    if d > 2.99 && d < 3.01 {
        println("  [PASS] clamp(5.5, 0.0, 3.0) returns f64 via using")
        passed += 1
    } otherwise {
        println("  [FAIL] clamp f64 via using: got ${d}")
        failed += 1
    }

    // Test 7: is_match via using (existing, regression check)
    if is_match("^hello", "hello world") {
        println("  [PASS] is_match via using")
        passed += 1
    } otherwise {
        println("  [FAIL] is_match via using")
        failed += 1
    }

    // Summary
    println("")
    println("Results: ${passed} passed, ${failed} failed")

    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
