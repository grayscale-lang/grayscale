import @arrays

do main() {
    mut x i64 = 10
    mut y i64 = 20
    mut z i64 = 30
    mut ptrs [^i64] = {}
    arrays.append(ptrs, addr(x))
    arrays.append(ptrs, addr(y))
    arrays.append(ptrs, addr(z))
    println(ptrs[0]^)
    println(ptrs[1]^)
    println(ptrs[2]^)
}
