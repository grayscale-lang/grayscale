/*
 * map_shorthand.gray - Test [K:V] shorthand syntax for maps
 */

import @maps

do get_map() -> [string:i64] {
    mut result [string:i64] = {"a": 1, "b": 2}
    return result
}

do take_map(m [string:i64]) -> i64 {
    return m["a"]
}

do main() {
    println("=== Map Shorthand [K:V] Test ===")
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: Variable declaration with [K:V]
    mut m1 [string:i64] = {"x": 10, "y": 20}
    if len(m1) == 2 {
        println("  [PASS] [K:V] variable declaration")
        passed += 1
    } otherwise {
        println("  [FAIL] [K:V] variable declaration")
        failed += 1
    }

    // Test 2: Function return type with [K:V]
    mut m2 [string:i64] = get_map()
    if m2["a"] == 1 {
        println("  [PASS] [K:V] function return type")
        passed += 1
    } otherwise {
        println("  [FAIL] [K:V] function return type")
        failed += 1
    }

    // Test 3: Function parameter with [K:V]
    mut m3 [string:i64] = {"a": 42}
    if take_map(m3) == 42 {
        println("  [PASS] [K:V] function parameter")
        passed += 1
    } otherwise {
        println("  [FAIL] [K:V] function parameter")
        failed += 1
    }

    // Test 4: Nested map — array of maps [[string:i64]]
    mut m4 [[string:i64]] = {{"a": 1}, {"b": 2}}
    if len(m4) == 2 {
        println("  [PASS] nested [[string:i64]]")
        passed += 1
    } otherwise {
        println("  [FAIL] nested [[string:i64]]")
        failed += 1
    }

    // Test 5: Nested map — map of maps [string:[string:i64]]
    mut m5 [string:[string:i64]] = {"outer": {"inner": 99}}
    if m5["outer"]["inner"] == 99 {
        println("  [PASS] nested [string:[string:i64]]")
        passed += 1
    } otherwise {
        println("  [FAIL] nested [string:[string:i64]]")
        failed += 1
    }

    // Test 6: Mixed — map[K:V] and [K:V] in same program
    mut m6 map[string:i64] = {"old": 1}
    mut m7 [string:i64] = {"new": 2}
    if m6["old"] == 1 && m7["new"] == 2 {
        println("  [PASS] mixed map[K:V] and [K:V]")
        passed += 1
    } otherwise {
        println("  [FAIL] mixed map[K:V] and [K:V]")
        failed += 1
    }

    // Test 7: Empty map with [K:V]
    mut m8 [string:i64] = {:}
    if len(m8) == 0 {
        println("  [PASS] empty [K:V] map")
        passed += 1
    } otherwise {
        println("  [FAIL] empty [K:V] map")
        failed += 1
    }

    println("")
    println("Results: ${passed} passed, ${failed} failed out of ${passed + failed}")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
