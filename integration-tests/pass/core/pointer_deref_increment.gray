do main() {
    mut passed = 0
    mut failed = 0

    mut x i64 = 10
    mut p ^i64 = addr(x)
    p^++
    if x == 11 { passed++ } otherwise { failed++ }

    mut y i64 = 5
    mut q ^i64 = addr(y)
    q^--
    if y == 4 { passed++ } otherwise { failed++ }

    println("passed: ${passed}, failed: ${failed}")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
