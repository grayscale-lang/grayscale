/*
 * type_param_generic.gray - <?> type-level parameters: a struct type name
 * passed as an argument drives new(T) and size_of(T).
 */

const Point struct {
    x i64
    y i64
}

const Color struct {
    r i64
    g i64
    b i64
}

do make(T <?>) -> ^? {
    return new(T)
}

do make_size(T <?>) -> i64 {
    return size_of(T)
}

do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    mut p = make(Point)
    p^.x = 10
    p^.y = 20
    if p^.x == 10 && p^.y == 20 {
        passed += 1
    } otherwise {
        println("[FAIL] make(Point)")
        failed += 1
    }

    mut c = make(Color)
    c^.r = 255
    c^.g = 128
    c^.b = 0
    if c^.r == 255 && c^.g == 128 && c^.b == 0 {
        passed += 1
    } otherwise {
        println("[FAIL] make(Color)")
        failed += 1
    }

    // Point has 2 i64 fields, Color has 3 — both positive, Color larger.
    const ps i64 = make_size(Point)
    const cs i64 = make_size(Color)
    if ps > 0 && cs > 0 && cs > ps {
        passed += 1
    } otherwise {
        println("[FAIL] make_size: Point=${ps} Color=${cs}")
        failed += 1
    }

    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
