/* Test: multiple named return values of different types, destructured at
 * the call site. */

do get_person_info() -> (age i64, name string) {
    mut age i64 = 25
    mut name string = "Bob"
    return age, name
}

do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    mut a i64, n string = get_person_info()
    if a == 25 {
        passed += 1
    } otherwise {
        println("[FAIL] first named return: ${a}")
        failed += 1
    }
    if n == "Bob" {
        passed += 1
    } otherwise {
        println("[FAIL] second named return: ${n}")
        failed += 1
    }

    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
