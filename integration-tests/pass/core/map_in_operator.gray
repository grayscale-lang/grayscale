/*
 * Test: map 'in' operator with various key types
 * Verifies map membership check works with correct C type casting
 */

do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: string key lookup
    mut colors map[string:i64] = {"red": 1, "green": 2, "blue": 3}
    if "red" in colors {
        println("  [PASS] string key 'red' found in map")
        passed += 1
    } otherwise {
        println("  [FAIL] string key 'red' not found in map")
        failed += 1
    }

    // Test 2: string key not present
    if "yellow" !in colors {
        println("  [PASS] string key 'yellow' not in map")
        passed += 1
    } otherwise {
        println("  [FAIL] string key 'yellow' unexpectedly in map")
        failed += 1
    }

    // Test 3: i64 key lookup
    mut ids map[i64:string] = {1: "alice", 2: "bob", 3: "charlie"}
    if 2 in ids {
        println("  [PASS] i64 key 2 found in map")
        passed += 1
    } otherwise {
        println("  [FAIL] i64 key 2 not found in map")
        failed += 1
    }

    // Test 4: i64 key not present
    if 99 !in ids {
        println("  [PASS] i64 key 99 not in map")
        passed += 1
    } otherwise {
        println("  [FAIL] i64 key 99 unexpectedly in map")
        failed += 1
    }

    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
