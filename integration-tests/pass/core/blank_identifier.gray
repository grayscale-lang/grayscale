/*
 * blank_identifier.gray - Test blank identifier _ for discarding values
 */


do get_pair() -> (i64, string) {
    return 42, "hello"
}

do get_triple() -> (i64, i64, i64) {
    return 1, 2, 3
}

do main() {
    println("=== Blank Identifier Test ===")
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: discard second return value
    mut val, _ = get_pair()
    if val == 42 {
        println("  [PASS] discard second value with _")
        passed += 1
    } otherwise {
        println("  [FAIL] expected 42, got ${val}")
        failed += 1
    }

    // Test 2: discard first return value
    mut _, str = get_pair()
    if str == "hello" {
        println("  [PASS] discard first value with _")
        passed += 1
    } otherwise {
        println("  [FAIL] expected hello, got ${str}")
        failed += 1
    }

    // Test 3: discard multiple values
    mut _, middle, _ = get_triple()
    if middle == 2 {
        println("  [PASS] discard first and third with _")
        passed += 1
    } otherwise {
        println("  [FAIL] expected 2, got ${middle}")
        failed += 1
    }

    // Summary
    println("")
    println("Results: ${passed} passed, ${failed} failed")

    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
