/*
 * c_interop.gray - Test C interop via extern import "header.h"
 *
 * Covers: calling C functions, accessing C constants,
 * multiple C header imports.
 */

extern import "stdio.h"
extern import "stdlib.h"

do main() {
    println("=== C Interop Test ===")
    mut passed int = 0
    mut failed int = 0

    // Test 1: call C function
    extern.puts("  hello from extern.puts")
    passed += 1

    // Test 2: access C constants
    if extern.EXIT_SUCCESS == 0 {
        println("  [PASS] extern.EXIT_SUCCESS == 0")
        passed += 1
    } otherwise {
        println("  [FAIL] extern.EXIT_SUCCESS")
        failed += 1
    }

    if extern.EXIT_FAILURE == 1 {
        println("  [PASS] extern.EXIT_FAILURE == 1")
        passed += 1
    } otherwise {
        println("  [FAIL] extern.EXIT_FAILURE")
        failed += 1
    }

    // Summary
    println("")
    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
