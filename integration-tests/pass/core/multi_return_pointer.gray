const Foo struct {
    x i64
}

do make_foo(n i64) -> (^Foo, Error) {
    mut f = new(Foo)
    f.x = n
    return f, nil
}

do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    mut r, e = make_foo(42)
    if e != nil {
        println("error")
        exit(1)
    }
    if r.x == 42 {
        passed = passed + 1
    } otherwise {
        println("[FAIL] multi-return pointer value wrong")
        failed = failed + 1
    }

    println("passed: ${passed}")
    println("failed: ${failed}")
}
