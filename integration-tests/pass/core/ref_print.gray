do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    mut x i64 = 42
    mut s string = "hello"
    mut f f64 = 3.14
    mut b bool = true

    mut rx = ref(x)
    mut rs = ref(s)
    mut rf = ref(f)
    mut rb = ref(b)

    if rx == 42 {
        println("  [PASS] println(ref(i64)) prints value")
        passed += 1
    } otherwise {
        println("  [FAIL] println(ref(i64)): expected 42")
        failed += 1
    }

    if rs == "hello" {
        println("  [PASS] println(ref(string)) prints value")
        passed += 1
    } otherwise {
        println("  [FAIL] println(ref(string)): expected hello")
        failed += 1
    }

    if rf == 3.14 {
        println("  [PASS] println(ref(f64)) prints value")
        passed += 1
    } otherwise {
        println("  [FAIL] println(ref(f64)): expected 3.14")
        failed += 1
    }

    if rb == true {
        println("  [PASS] println(ref(bool)) prints value")
        passed += 1
    } otherwise {
        println("  [FAIL] println(ref(bool)): expected true")
        failed += 1
    }

    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
