/*
 * map_non_standard_keys.gray - Maps with bool, char, u8, and f64 keys
 *
 * map codegen only handled string and i64
 * keys. bool/char/u8/f64 keys passed typechecking but emitted GrayString
 * as the C key type, causing compile errors or runtime key-not-found panics.
 */

do main() {
    println("=== Non-Standard Map Keys Test ===")
    mut passed i64 = 0
    mut failed i64 = 0

    // -- bool keys --
    mut bmap map[bool:string] = {true: "yes", false: "no"}
    if bmap[true] == "yes" && bmap[false] == "no" {
        println("  [PASS] bool key literal access")
        passed += 1
    } otherwise {
        println("  [FAIL] bool key literal access")
        failed += 1
    }

    bmap[true] = "affirmative"
    if bmap[true] == "affirmative" {
        println("  [PASS] bool key assignment update")
        passed += 1
    } otherwise {
        println("  [FAIL] bool key assignment update")
        failed += 1
    }

    // -- char keys --
    mut cmap map[char:i64] = {'a': 1, 'b': 2, 'c': 3}
    if cmap['a'] == 1 && cmap['b'] == 2 && cmap['c'] == 3 {
        println("  [PASS] char key literal access")
        passed += 1
    } otherwise {
        println("  [FAIL] char key literal access")
        failed += 1
    }

    cmap['d'] = 4
    if cmap['d'] == 4 && len(cmap) == 4 {
        println("  [PASS] char key insertion")
        passed += 1
    } otherwise {
        println("  [FAIL] char key insertion")
        failed += 1
    }

    // -- u8 keys --
    mut ymap map[u8:string] = {0b1: "one", 0b10: "two", 0b11: "three"}
    if ymap[0b1] == "one" && ymap[0b10] == "two" && ymap[0b11] == "three" {
        println("  [PASS] u8 key literal access")
        passed += 1
    } otherwise {
        println("  [FAIL] u8 key literal access")
        failed += 1
    }

    ymap[0b100] = "four"
    if ymap[0b100] == "four" && len(ymap) == 4 {
        println("  [PASS] u8 key insertion")
        passed += 1
    } otherwise {
        println("  [FAIL] u8 key insertion")
        failed += 1
    }

    // -- f64 keys --
    mut fmap map[f64:i64] = {1.5: 10, 2.5: 20, 3.5: 30}
    if fmap[1.5] == 10 && fmap[2.5] == 20 && fmap[3.5] == 30 {
        println("  [PASS] f64 key literal access")
        passed += 1
    } otherwise {
        println("  [FAIL] f64 key literal access")
        failed += 1
    }

    fmap[4.5] = 40
    if fmap[4.5] == 40 && len(fmap) == 4 {
        println("  [PASS] f64 key insertion")
        passed += 1
    } otherwise {
        println("  [FAIL] f64 key insertion")
        failed += 1
    }

    // Summary
    println("")
    println("Results: ${passed} passed, ${failed} failed")

    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
