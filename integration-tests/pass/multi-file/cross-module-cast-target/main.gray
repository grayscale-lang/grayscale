import "./types.gray"

do main() {
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: cast to a module-qualified enum
    mut n i64 = 1
    mut c types.Color = cast(n, types.Color)
    if c == types.Color.GREEN {
        println("  [PASS] cast to module-qualified enum")
        passed += 1
    } otherwise {
        println("  [FAIL] cast to module-qualified enum")
        failed += 1
    }

    // Test 2: cast from a module-qualified enum back to i64
    mut back i64 = cast(types.Color.BLUE, i64)
    if back == 2 {
        println("  [PASS] cast module-qualified enum to i64")
        passed += 1
    } otherwise {
        println("  [FAIL] cast module-qualified enum to i64: got ${back}")
        failed += 1
    }

    // Test 3: cast to a module-qualified alias
    mut s types.Score = cast(3.7, types.Score)
    if s == 3 {
        println("  [PASS] cast to module-qualified alias")
        passed += 1
    } otherwise {
        println("  [FAIL] cast to module-qualified alias: got ${s}")
        failed += 1
    }

    println("Results: ${passed} passed, ${failed} failed")
    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
