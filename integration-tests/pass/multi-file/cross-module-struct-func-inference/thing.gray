const Thing struct {
    id string = "x"

    do ratio(t Thing) -> f64 { return 2.5 }
    do ok(t Thing) -> bool { return true }
    do label(t Thing) -> string { return "thing-${t.id}" }
    do count(t Thing) -> i64 { return 7 }
}
