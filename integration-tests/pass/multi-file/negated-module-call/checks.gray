/*
 * checks.gray - User module with bool-returning functions
 */


do is_positive(n i64) -> bool {
    return n > 0
}

do is_empty(s string) -> bool {
    return len(s) == 0
}

do is_in_range(val i64, low i64, high i64) -> bool {
    return val >= low && val <= high
}
