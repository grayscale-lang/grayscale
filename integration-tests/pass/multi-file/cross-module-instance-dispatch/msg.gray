/*
 * msg.gray - a struct with functions taking each receiver form.
 */

const Msg struct {
    id i64

    do describe(m Msg) -> string {
        return "msg-${m.id}"
    }

    do bump(&m Msg) {
        m.id = m.id + 1
    }

    do peek(m ^Msg) -> i64 {
        return m^.id
    }
}
