const Item struct {
    name string
    weight i64
}

do find_heaviest(items [Item]) -> Item {
    mut best Item = items[0]
    for_each i, item in items {
        if item.weight > best.weight {
            best = item
        }
    }
    return best
}
