const VERSION string = "1.0.0"

do greet(name string) -> string {
    return "Hello, ${name}"
}

mut tally i64 = 5

private const HIDDEN i64 = 9

do bump() -> i64 {
    tally += 1
    return tally + HIDDEN
}
