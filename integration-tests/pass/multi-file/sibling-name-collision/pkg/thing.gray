import "./color.gray"

const Thing struct {
    color Color
}

do describe(color Color) -> i64 {
    if color == Color.RED { return 1 }
    return 0
}

do make_thing(color Color) -> Thing {
    return Thing{color: color}
}

do local_shadow() -> i64 {
    mut color i64 = 7
    return color
}
