import "./state.gray"

do main() {
    state.increment()
    state.increment()
    mut count i64 = state.get_count()
    if count == 2 {
        println("ALL TESTS PASSED")
    } otherwise {
        println("SOME TESTS FAILED")
    }
}
