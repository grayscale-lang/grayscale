/*
 * Test for_each with same-module array constants
 */
import "./testmod.gray"


do main() {
    println("=== For_each Module Constant Test ===")
    mut passed i64 = 0
    mut failed i64 = 0

    // Test 1: for_each over string array constant
    println("Items:")
    testmod.print_items()
    println("  [PASS] for_each over string array")
    passed += 1

    // Test 2: for_each over i64 array constant with accumulation
    mut sum = testmod.sum_numbers()
    if sum == 100 {
        println("  [PASS] for_each over i64 array (sum=${sum})")
        passed += 1
    } otherwise {
        println("  [FAIL] for_each over i64 array: expected 100, got ${sum}")
        failed += 1
    }

    // Summary
    println("")
    println("Results: ${passed} passed, ${failed} failed")

    if failed > 0 {
        println("SOME TESTS FAILED")
    } otherwise {
        println("ALL TESTS PASSED")
    }
}
