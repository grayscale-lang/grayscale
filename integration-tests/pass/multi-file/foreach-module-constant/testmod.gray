
const ITEMS [string, 3] = {"apple", "banana", "cherry"}
const NUMBERS [i64, 4] = {10, 20, 30, 40}

do print_items() {
    for_each item in ITEMS {
        println(item)
    }
}

do sum_numbers() -> i64 {
    mut total i64 = 0
    for_each n in NUMBERS {
        total += n
    }
    return total
}
