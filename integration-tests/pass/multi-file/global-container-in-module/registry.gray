import @arrays

mut keys map[string:int] = {:}
mut names [string] = {}

do register(name string) {
    keys[name] = 1
}

do add_name(s string) {
    arrays.append(names, s + "!")
}

do has_key(name string) -> bool {
    return keys[name] == 1
}

do first_name() -> string {
    return names[0]
}
