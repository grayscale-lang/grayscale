const Tag enum {
    Leaf(i64)
    Nil
}

const Plain enum {
    LOW
    HIGH
}

do tagged(n i64) -> (Tag, Error) {
    if n < 0 { return Tag.Nil, error("neg") }
    return Tag.Leaf(n), nil
}

do plain(n i64) -> (Plain, i64) {
    if n > 10 { return Plain.HIGH, n }
    return Plain.LOW, n
}
