/*
 * types.gray - Nested struct definitions for cross-module test
 */


const Inner struct {
    value i64
    name string
}

const Outer struct {
    inner Inner
    count i64
}

const Deep struct {
    outer Outer
    label string
}
