/*
 * Error Test: E3127 - a primitive passed as a type argument through a
 * module-qualified call. The qualified path skipped type-argument validation
 * entirely, so this compiled with no diagnostic at all.
 * Expected: "'int' is not a struct"
 */
// expect-error: E3127
// expect-error-count: 1

import "./utils.gray"

do main() {
    mut x = utils.new_T(int)
    println(x)
}
