/*
 * Error Test: E3128 - a module-qualified type argument through a
 * module-qualified generic call. The qualified path skipped validation, so
 * this reached the C compiler instead of being rejected.
 * Expected: "got a non-type expression"
 */
// expect-error: E3128
// expect-error-count: 1

import "./utils.gray"
import "./types.gray"

do main() {
    mut x = utils.new_T(types.Actor)
    println(x.id)
}
