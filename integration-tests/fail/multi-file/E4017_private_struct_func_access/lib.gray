/*
 * lib.gray - Library with a private struct function
 */

const Box struct {
    v i64

    private do secret(self Box) -> i64 {
        return self.v * 2
    }

    do open(self Box) -> i64 {
        return Box.secret(self)
    }
}
