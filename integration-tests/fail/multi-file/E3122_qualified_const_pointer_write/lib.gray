const Config struct {
    port int
}

const FROZEN Config = Config{port: 1}
const NUMS [int, 3] = {1, 2, 3}

mut live Config = Config{port: 1}
mut live_nums [int] = {1, 2, 3}

do show() {
    println(FROZEN.port)
}
