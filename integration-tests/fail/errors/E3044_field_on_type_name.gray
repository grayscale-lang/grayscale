/*
 * Error Test: E3044 - field access on struct type name
 * Expected: "cannot access field" and "use an instance variable"
 */

const Point struct {
    x i64
    y i64
}

do main() {
    mut val i64 = Point.x
}
