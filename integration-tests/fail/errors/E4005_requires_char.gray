/*
 * Error Test: E4005 - requires-char
 * Expected: "must be a char" or "requires char"
 */

import @strings

do main() {
    mut idx i64 = strings.index_char("hello", "ll")  // string instead of char
}
