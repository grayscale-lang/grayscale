/*
 * Error Test: E3121 - a collection cannot be a when condition
 */

// expect-error: E3121

do main() {
    mut arr [int] = {1, 2, 3}
    when arr {
        is 1 { println("match") }
        default { println("no match") }
    }
}
