/*
 * Error Test: E3044 - assigning to field on struct type name
 * Expected: "cannot access field" and "use an instance variable"
 */

const Person struct {
    name string
    age i64
}

do main() {
    Person.name = "Alice"
}
