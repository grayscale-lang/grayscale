/*
 * Error Test: E3002 - division by literal integer zero
 *
 * `const x i64 = 10 / 0` used to compile and
 * silently produce undefined behavior. Typechecker now catches the
 * literal-zero divisor at compile time.
 */

do main() {
    mut x i64 = 10 / 0
}
