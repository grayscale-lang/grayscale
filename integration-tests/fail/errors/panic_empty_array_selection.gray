/*
 * Error Test: E9007 - empty-array-selection
 * Expected: "empty array" or "cannot select"
 */

import @random

do main() {
    mut arr [i64] = {}
    mut x i64 = random.choice(arr)  // ERROR: cannot select from empty array
}
