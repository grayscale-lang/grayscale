/*
 * Error Test: E3045 - or_return on function that doesn't return error
 * Expected: "or_return requires a function that returns (T, Error)"
 */

do get_value() -> i64 {
    return 42
}

do main() -> (i64, Error) {
    mut v = get_value() or_return
    return v, nil
}
