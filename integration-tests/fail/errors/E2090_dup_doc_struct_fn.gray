// expect-error: E2090
// expect-error-count: 1
// Duplicate #doc on a struct function.

const Calc struct {
    _pad int

    #doc("Multiplies")
    #doc("Two numbers")
    do mul(a int, b int) -> int {
        return a * b
    }
}

do main() {}
