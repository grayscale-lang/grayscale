// expect-error: E2090
// expect-error-count: 1
// Duplicate #doc on a struct function.

const Calc struct {
    _pad i64

    #doc("Multiplies")
    #doc("Two numbers")
    do mul(a i64, b i64) -> i64 {
        return a * b
    }
}

do main() {}
