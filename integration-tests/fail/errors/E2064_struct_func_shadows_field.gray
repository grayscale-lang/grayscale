/*
 * Error Test: E2064 - struct function name conflicts with a field name
 * Expected: "function 'x' conflicts with field 'x' in struct 'Point'" error
 */

const Point struct {
    x i64
    y i64

    do x() -> i64 {
        return 0
    }
}

do main() {
    mut p = new(Point)
}
