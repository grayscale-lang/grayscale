/*
 * Error Test: E5026 - strings.split_n third argument must be an i64 count
 */
// expect-error: E5026

import @strings

do main() {
    mut parts [string] = strings.split_n("a=b=c", "=", "2")
    println(len(parts))
}
