/*
 * Error Test: E2001 - for_each with opening paren but missing closing paren
 * Expected: "expected ), got { instead"
 */

do main() {
    mut items [i64] = {1, 2, 3}
    for_each (item in items {
        mut x i64 = item
    }
}
