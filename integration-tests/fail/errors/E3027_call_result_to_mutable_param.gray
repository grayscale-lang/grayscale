/*
 * Error Test: E3027 - function call result passed to mutable (&) parameter
 * Expected: "cannot pass a literal or expression to mutable parameter" error
 */

do get_val() -> i64 {
    return 5
}

do modify(&x i64) {
    x = 99
}

do main() {
    modify(get_val())
}
