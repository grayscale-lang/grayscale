/*
 * Error Test: E5024 - return-type-mismatch (runtime)
 * Expected: "return type mismatch"
 */


do get_value() -> u64 {
    mut x i64 = -1
    return x  // Runtime error: i64 -1 cannot be returned as u64
}

do main() {
    mut val u64 = get_value()
    println(val)
}
