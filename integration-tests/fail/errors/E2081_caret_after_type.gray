/*
 * Error Test: E2081 - caret after type name instead of before
 * Expected: "'^' is a dereference operator, not a type modifier"
 */

do foo() -> i64^ {
    mut x i64 = 42
    return addr(x)
}

do main() {
    foo()
}
