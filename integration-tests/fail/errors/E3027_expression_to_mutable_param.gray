/*
 * Error Test: E3027 - expression passed to mutable (&) parameter
 * Expected: "cannot pass a literal or expression to mutable parameter" error
 */

do modify(&x i64) {
    x = 99
}

do main() {
    modify(1 + 2)
}
