// Error Test: E3019 - instance-dispatch call must run the same signedness guard as Type.func()
// expect-error: E3019
// expect-error-count: 1

const V struct {
    n i64

    do take(v V, x i64) -> i64 {
        return x
    }
}

do main() {
    mut u u64 = 18446744073709551615
    mut a V = V{ n: 0 }
    println("${a.take(u)}")
}
