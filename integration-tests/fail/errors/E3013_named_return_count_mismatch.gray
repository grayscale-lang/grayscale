/* Test: Named return with wrong number of values
 * Expected: E3013 error - wrong number of return values
 */


do getValues() -> (age i64, name string) {
    return 42
}

do main() {
    mut a i64, n string = getValues()
    println(a)
}
