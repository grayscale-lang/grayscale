/*
 * Error Test: E4007 - struct name collides with existing enum name
 * Expected: "a type named 'Color' is already declared" error
 */

const Color enum {
    RED
    GREEN
    BLUE
}

const Color struct {
    r i64
    g i64
    b i64
}

do main() {
    mut c = Color.RED
}
