/*
 * Error Test: E3001 - map-key-type-mismatch
 * Expected: "map key type mismatch"
 */


do main() {
    mut m map[string:i64] = {"a": 1, "b": 2}
    mut val = m[123]  // Should fail: i64 key on string-keyed map
}
