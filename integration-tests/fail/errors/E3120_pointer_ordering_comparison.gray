/* Error Test: E3120 - ordering comparison on pointer types */
do main() {
    mut x i64 = 10
    mut y i64 = 20
    mut px ^i64 = addr(x)
    mut py ^i64 = addr(y)
    if px < py { println("less") }
}
