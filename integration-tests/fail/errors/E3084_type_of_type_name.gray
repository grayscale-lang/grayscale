/*
 * Error Test: E3084 - type_of() with a type name instead of a value
 * Expected: E3084 "type_of() expects a value, not a type name"
 */

const Point struct {
    x i64
    y i64
}

do main() {
    println(type_of(Point))
}
