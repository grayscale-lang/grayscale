/*
 * Error Test: E4001 - while-scope-leak
 * Expected: "undefined variable 'inner'"
 */

do main() {
    mut x i64 = 0
    while x < 1 {
        mut inner i64 = 42
        x++
    }
    println("${inner}")
}
