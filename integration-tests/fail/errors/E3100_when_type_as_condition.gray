/*
 * Error Test: E3100 - a type name cannot be a when subject
 */

// expect-error: E3100

const COLOR enum {
    RED
    GREEN
    BLUE
}

do main() {
    when COLOR {
        is COLOR.RED { println("Red") }
        default { println("Wrong") }
    }
}
