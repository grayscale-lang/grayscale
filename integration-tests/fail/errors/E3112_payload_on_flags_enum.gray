/*
 * Error Test: E3112 - payload on #flags enum variant
 * Expected: "payload types are not allowed on #flags enum variants"
 */

#flags
const Perms enum {
    READ(i64)
    WRITE
    EXEC
}

do main() {}
