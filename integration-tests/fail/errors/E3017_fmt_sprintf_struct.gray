/*
 * Error Test: E3017 - fmt.sprintf cannot format struct
 * Expected: "cannot format value of type"
 */

import @fmt

const Color struct {
    r i64
    g i64
    b i64
}

do main() {
    mut c Color = Color{r: 255, g: 0, b: 0}
    mut s string = fmt.sprintf("color: %s", c)
    println(s)
}
