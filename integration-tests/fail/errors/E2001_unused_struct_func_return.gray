/*
 * Error Test: E2001 - struct function call syntax
 * Expected: E2001 unexpected symbol (parser rejects instance.func() syntax)
 */

const Math struct {
    x i64
}

do Math.add(a i64, b i64) -> i64 {
    return a + b
}

do main() {
    Math.add(1, 2)
}
