/* Error Test: E2094 - #deprecated on a field must not drift onto the next
 * struct function in the body */
// expect-error: E2094
const Point struct {
    #deprecated
    x i64

    do sum() -> i64 { return 3 }
}

do main() {
    println("${Point.sum()}")
}
