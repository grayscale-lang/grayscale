/*
 * Error Test: E5007 - modifying through a const ref
 * Expected: compiler should reject this with E5007
 */

import @arrays

do main() {
    mut arr [i64] = {1, 2, 3}
    const r = ref(arr)
    arrays.append(r, 4)  // ERROR: cannot modify through const ref
}
