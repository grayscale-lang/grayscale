/*
 * Error Test: E3080 - named return must return the named variable
 * Expected: "function must return named variable 'result'"
 */

do add(a i64, b i64) -> (result i64) {
    mut result i64 = a + b
    return 99
}

do main() {
    add(1, 2)
}
