/*
 * Error Test: E3001 - wrong argument type on struct function
 * Expected: "expected i64, got string" error
 */

const Point struct {
    x i64
    y i64

    do scale(factor i64) -> i64 {
        return 0
    }
}

do main() {
    mut result = Point.scale("hello")
}
