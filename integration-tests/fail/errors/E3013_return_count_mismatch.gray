/*
 * Error Test: E3013 - return-count-mismatch
 * Expected: "wrong number of return" or "count mismatch"
 */

do getValue() -> (i64, i64) {
    return 1  // expected 2 values
}

do main() {
    mut a i64, b i64 = getValue()
}
