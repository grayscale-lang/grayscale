/*
 * Error Test: E3161 - ref() rejects a dynamic array index expression;
 * the backing store relocates when the array grows.
 */
// expect-error: E3161
// expect-error-count: 1

do main() {
    mut a [int] = {10, 20, 30, 40}
    mut r = ref(a[1])
}
