/*
 * Error Test: E3166 - double mem.destroy() on the same arena
 *
 * compile-time detection of straight-line double
 * destroy on the same variable.
 */

import @mem

do main() {
    mut a = mem.arena(1024)
    mem.destroy(a)
    mem.destroy(a)
}
