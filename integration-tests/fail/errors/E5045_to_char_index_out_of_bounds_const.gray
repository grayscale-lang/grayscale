/*
 * Error Test: E5045 - constant argument outside a builtin's domain
 * to_char() with a constant index past the end of a string literal (runtime P0050).
 *
 * expect-error: E5045
 */

do main() {
    println(to_char("abc", 9))
}
