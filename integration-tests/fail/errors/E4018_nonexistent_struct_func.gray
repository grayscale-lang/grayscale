/*
 * Error Test: E4018 - calling a function that doesn't exist on a struct
 * Expected: "struct 'Point' has no function named 'doesnt_exist'"
 */

const Point struct {
    x i64
    y i64
}

do main() {
    Point.doesnt_exist(1, 2, 3)
}
