/*
 * Error Test: E5008 - wrong-argument-count
 * Expected: "wrong number of arguments"
 */

do add(a i64, b i64) -> i64 {
    return a + b
}

do main() {
    mut x i64 = add(1)  // Missing second argument
}
