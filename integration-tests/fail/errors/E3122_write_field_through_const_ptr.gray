/* Error Test: E3122 - cannot modify field through pointer to const-declared struct */
const Point struct {
    x i64
    y i64
}
do main() {
    const pt Point = Point{x: 1, y: 2}
    mut p ^Point = addr(pt)
    p^.x = 99
}
