/*
 * Error Test: E3001 - returning ^^^i64 from a -> ^i64 function
 *
 * same check at deeper nesting.
 */

do bad() -> ^i64 {
    mut x i64 = 42
    mut p ^i64 = addr(x)
    mut pp ^^i64 = addr(p)
    mut ppp ^^^i64 = addr(pp)
    return ppp
}

do main() {
    mut r ^i64 = bad()
    println(r^)
}
