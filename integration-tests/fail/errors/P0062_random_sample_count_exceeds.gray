// expect-contains: .gray:
/* Panic Test: P0062 - random.sample count exceeds array length */
import @random

do main() {
    mut arr [i64] = {1, 2, 3}
    mut result [i64] = random.sample(arr, 10)
    println(result)
}
