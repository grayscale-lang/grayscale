// expect-contains: .gray:
/* Panic Test: P0062 - random.sample count exceeds array length */
import @random

do main() {
    mut arr [int] = {1, 2, 3}
    mut result [int] = random.sample(arr, 10)
    println(result)
}
