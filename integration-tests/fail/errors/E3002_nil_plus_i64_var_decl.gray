/*
 * Error Test: E3002 - nil used as operand of '+' in var_decl
 *
 * used to fire the misleading "cannot
 * assign nil to i64" because the infix resolver returned nil
 * as the expression type. Now reports the real issue (nil as
 * '+' operand) and the cascade suppressor prevents a
 * second assignment-mismatch diagnostic.
 */

do main() {
    mut x i64 = nil + 1
}
