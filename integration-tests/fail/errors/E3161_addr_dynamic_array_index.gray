/*
 * Error Test: E3161 - addr() rejects a dynamic array index expression;
 * the backing store relocates when the array grows, leaving the pointer
 * dangling.
 */
// expect-error: E3161
// expect-error-count: 1

do main() {
    mut a [i64] = {10, 20, 30, 40}
    mut p ^i64 = addr(a[1])
}
