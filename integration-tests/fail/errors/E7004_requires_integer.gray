/*
 * Error Test: E7004 - requires-integer
 * Expected: "must be an integer" or "requires integer"
 */

import @strings

do main() {
    mut s string = strings.repeat("x", 3.5)  // f64 instead of i64
}
