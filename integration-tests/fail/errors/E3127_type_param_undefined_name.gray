/*
 * Error Test: E3127 - type argument names no type at all
 * Expected: "type parameter expects a struct type name, but 'Undefined' is not a struct"
 */

do make(T <?>) -> ^? {
    return new(T)
}

do main() {
    mut p = make(Undefined)
}
