/*
 * Error Test: E9004 - arrays.filter() callback must return bool
 * Expected: E9004 error
 */

import @arrays

do not_bool(x i64) -> i64 { return x * 2 }

do main() {
    mut nums [i64] = {1, 2, 3}
    mut result [i64] = arrays.filter(nums, ()not_bool)
}
