// expect-error: E2090
// expect-error-count: 1
// Duplicate #discard on a struct function.

const Calc struct {
    _pad i64

    #discard
    #discard
    do add(a i64, b i64) -> i64 {
        return a + b
    }
}

do main() {}
