// expect-error: E2090
// expect-error-count: 1
// Duplicate #discard on a struct function.

const Calc struct {
    _pad int

    #discard
    #discard
    do add(a int, b int) -> int {
        return a + b
    }
}

do main() {}
