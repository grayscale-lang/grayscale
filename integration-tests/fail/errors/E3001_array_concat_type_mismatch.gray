/*
 * Error Test: E3001 - array-concat-type-mismatch
 * Expected: "type mismatch" or "element type"
 */

import @arrays

do main() {
    mut nums [i64] = {1, 2, 3}
    mut strs [string] = {"a", "b"}
    mut result = arrays.concat(nums, strs)  // Should fail: incompatible element types
}
