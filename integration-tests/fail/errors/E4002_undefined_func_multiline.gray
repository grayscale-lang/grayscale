/*
 * Error Test: E4002 - undefined function on a later line
 * Expected: error points at the function name, not the parenthesis
 * caret should be on the function name
 */

do helper() -> i64 {
    return 42
}

do main() {
    mut x i64 = helper()
    mut y i64 = does_not_exist()
    println(x + y)
}
