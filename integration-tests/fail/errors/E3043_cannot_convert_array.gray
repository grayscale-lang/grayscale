/*
 * Error Test: E3043 - cannot-convert-array
 * Expected: "cannot convert [i64] to i64; only numeric types, strings, and bools can be converted"
 */

do main() {
    mut arr [i64] = {1, 2, 3}
    mut x i64 = cast(arr, i64)  // cannot convert array to i64
}
