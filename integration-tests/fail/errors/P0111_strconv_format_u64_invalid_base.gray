/* Panic Test: P0111 - strconv.format_u64 with invalid base at runtime */
import @strconv

do get_base() -> i64 {
    return 99
}

do main() {
    mut n u64 = 255
    mut base i64 = get_base()
    mut s string = strconv.format_u64(n, base)
    println(s)
}
