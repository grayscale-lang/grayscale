/*
 * Error Test: E3013 - returning multiple values from single-return function
 * Expected: E3013 wrong number of return values
 */

do add(a i64, b i64) -> i64 {
    return a, b
}

do main() {
    mut r i64 = add(1, 2)
    println("${r}")
}
