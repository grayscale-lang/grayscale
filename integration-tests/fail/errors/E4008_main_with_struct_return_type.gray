/*
 * Error Test: E4008 - main declared with a user-defined struct return type
 *
 * struct return types on main are rejected at
 * E4008 without cascading into struct-specific checks.
 */

const Foo struct {
    x i64
}

do main() -> Foo {
}
