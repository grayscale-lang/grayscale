/*
 * Error Test: E1015 - underscore after decimal (multiple digits before)
 * Expected: "underscore" or "decimal"
 * Tests that 123._456 is caught
 */

do main() {
    mut x f64 = 123._456  // underscore after decimal
}
