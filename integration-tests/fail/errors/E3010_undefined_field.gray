/*
 * Error Test: E3010 - undefined-field
 * Expected: "struct 'Point' has no field 'z'"
 */

const Point struct {
    x i64
    y i64
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    println(p.z)  // Field 'z' doesn't exist
}
