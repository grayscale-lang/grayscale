/*
 * Error Test: E2002 - duplicate struct field name (uses old 'struct' syntax)
 * Expected: E2002 unexpected symbol
 */

struct Point {
    x i64,
    y i64,
    x i64,
}

do main() {
    mut p Point = Point{x: 1, y: 2}
}
