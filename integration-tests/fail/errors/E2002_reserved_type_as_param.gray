/*
 * Error Test: E2002 - reserved type name used as parameter name
 * Expected: "'bool' is a built-in name and cannot be used as a parameter name"
 */

do check(bool i64) -> i64 {
    return bool
}

do main() {
    mut x = check(1)
}
