/*
 * Error Test: E3013 - ref()/raw() also reject a string index expression,
 * matching addr().
 */
// expect-error: E3013
// expect-error-count: 2

do main() {
    mut s string = "hi"
    mut p ^char = raw(s[0])
    mut q ^char = ref(s[1])
}
