/* Error Test: E2002 - #deprecated on a field must not drift onto the next
 * struct function in the body */
// expect-error: E2002
const Point struct {
    #deprecated
    x int

    do sum() -> int { return 3 }
}

do main() {
    println("${Point.sum()}")
}
