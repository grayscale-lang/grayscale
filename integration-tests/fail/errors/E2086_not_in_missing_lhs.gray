/*
 * Error Test: E2086 - 'not_in' used without a left-hand value
 * Expected: "'not_in' requires a value on the left side"
 */

do main() {
    mut arr [i64] = {1, 2, 3}
    if not_in arr {
        println("test")
    }
}
