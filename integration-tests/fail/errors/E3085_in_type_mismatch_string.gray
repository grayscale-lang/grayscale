/*
 * Error Test: E3085 - 'in' operator type mismatch (i64 in string)
 * Expected: E3085 "'in' operator type mismatch"
 */

do main() {
    mut s string = "hello"
    if 42 in s {
        println("found")
    }
}
