/*
 * Error Test: E2066 - struct field has same name as its struct type
 * Expected: "struct field 'Foo' cannot have the same name as its struct type 'Foo'" error
 */

const Foo struct {
    Foo i64
    bar string
}

do main() {
    mut f = new(Foo)
}
