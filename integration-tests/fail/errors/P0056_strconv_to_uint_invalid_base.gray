/* Panic Test: P0056 - strconv.to_uint with invalid base at runtime */
import @strconv

do get_base() -> int {
    return 0
}

do main() {
    mut base int = get_base()
    mut val, mut err = strconv.to_uint("42", base)
    println(val)
}
