/*
 * Error Test: E5045 - constant argument outside a stdlib function's domain
 * math.sqrt of a negative constant is decidable at compile time (runtime P0064).
 *
 * expect-error: E5045
 */

import @math

do main() {
    println(math.sqrt(-4.0))
}
