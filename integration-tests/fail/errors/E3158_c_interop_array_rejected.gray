/*
 * Error Test: E3001 - arrays cannot be passed to C functions
 * Expected: error about array at C boundary
 */

extern import "stdio.h"

do main() {
    mut arr [int] = {1, 2, 3}
    extern.puts(arr)
}
