/*
 * Error Test: E9004 - arrays.filter() callback parameter type mismatch
 * Expected: E9004 error
 */

import @arrays

do string_cb(s string) -> bool { return s == "hello" }

do main() {
    mut nums [i64] = {1, 2, 3}
    mut result [i64] = arrays.filter(nums, ()string_cb)
}
