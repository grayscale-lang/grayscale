/*
 * Error Test: E3001 - return-type-mismatch
 * Expected: "return type mismatch"
 */

do get_number() -> i64 {
    return "hello"  // Returning string, declared i64
}

do main() {
    mut x i64 = get_number()
}
