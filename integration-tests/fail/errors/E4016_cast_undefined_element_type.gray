/*
 * Error Test: E4016 - cast() to an array whose element type does not exist
 * Expected: "undefined type 'Nope'"
 */

// expect-error: E4016
// expect-error-count: 1

do main() {
    mut a [int] = {1}
    println(len(cast(a, [Nope])))
}
