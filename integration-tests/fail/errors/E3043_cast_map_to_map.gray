/*
 * Error Test: E3043 - cast() to a map type
 * Expected: E3043 "cannot cast" error
 */

// expect-error: E3043
// expect-error-count: 1

do main() {
    mut m map[string:i64] = {"a": 1}
    mut n map[string:i64] = cast(m, map[string:i64])
    println(len(n))
}
