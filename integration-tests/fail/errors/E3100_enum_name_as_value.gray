/*
 * Error Test: E3100 - an enum type name used as a value
 * Expected: "type name 'Color' cannot be used as a value"
 */

// expect-error: E3100
// expect-error-count: 1

const Color enum {
    RED
    GREEN
}

do main() {
    println(Color)
}
