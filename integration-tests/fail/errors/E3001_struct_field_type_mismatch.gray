/*
 * Error Test: E3001 - struct field type mismatch in literal
 * Expected: compiler should reject this with E3001
 */

const Person struct {
    name string
    age i64
}

do main() {
    mut p = Person{name: 123, age: "hello"}
}
