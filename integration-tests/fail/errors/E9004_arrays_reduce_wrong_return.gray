/*
 * Error Test: E9004 - arrays.reduce() callback must return same type as accumulator
 * Expected: E9004 error
 */

import @arrays

do bad_reducer(acc i64, x i64) -> string { return "nope" }

do main() {
    mut nums [i64] = {1, 2, 3}
    mut result = arrays.reduce(nums, 0, ()bad_reducer)
}
