/*
 * Error Test: E3162 - cannot return addr() of a local variable
 *
 * the local's memory is freed when the function
 * returns, so the pointer would dangle.
 */

do bad() -> ^i64 {
    mut x i64 = 42
    return addr(x)
}

do main() {
    mut p ^i64 = bad()
    println(p^)
}
