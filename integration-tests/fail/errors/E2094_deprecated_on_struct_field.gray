/* Error Test: E2094 - #deprecated applied to a struct field */
// expect-error: E2094
const Point struct {
    #deprecated
    x i64
    y i64
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    println("${p.x}")
}
