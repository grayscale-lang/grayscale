/*
 * Error Test: E3006 - multi-assign-count-mismatch
 * Expected: "too many variables; the function returns N value(s) but variable N was requested"
 */

do getTwo() -> (i64, i64) {
    return 1, 2
}

do main() {
    mut a i64, b i64, c i64 = getTwo()  // 3 vars, 2 values
}
