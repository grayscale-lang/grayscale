/*
 * Error Test: E4007 - enum name collides with existing struct name
 * Expected: "a type named 'Shape' is already declared" error
 */

const Shape struct {
    sides i64
}

const Shape enum {
    CIRCLE
    SQUARE
}

do main() {
    mut s = new(Shape)
}
