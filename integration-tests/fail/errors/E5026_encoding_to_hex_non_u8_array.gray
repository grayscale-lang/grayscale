/*
 * Error Test: E5026 - encoding.to_hex requires a [u8] array, not just any array
 * Expected: encoding.to_hex() expects [u8] as argument 1, got [i64]
 */

// expect-error: E5026

import @encoding

do main() {
    mut xs [i64] = {65, 66, 67}
    println(encoding.to_hex(xs))
}
