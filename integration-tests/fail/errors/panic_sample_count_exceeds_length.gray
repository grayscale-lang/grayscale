/*
 * Error Test: E9008 - sample-count-exceeds-length
 * Expected: "sample" or "exceeds" or "length"
 */

import @random

do main() {
    mut arr [i64] = {1, 2, 3}
    mut sampled = random.sample(arr, 10)  // ERROR: sample count exceeds array length
}
