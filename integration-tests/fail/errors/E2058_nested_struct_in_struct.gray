// Test: E2058 - nested struct declaration inside struct
// Expected: "cannot declare a struct or enum inside struct"

const Outer struct {
    x i64
    const Inner struct {
        y i64
    }
}

do main() {
    println("should not compile")
}
