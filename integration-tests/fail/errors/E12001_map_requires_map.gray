/*
 * Error Test: E12001 - map-requires-map
 * Expected: "requires a map argument"
 */

import @maps

do main() {
    mut arr [i64] = {1, 2, 3}
    mut keys [i64] = maps.get_keys(arr)  // Passing array to map function
}
