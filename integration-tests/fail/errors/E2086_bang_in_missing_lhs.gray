/*
 * Error Test: E2086 - '!in' used without a left-hand value
 * Expected: "'!in' requires a value on the left side"
 */

do main() {
    mut arr [i64] = {1, 2, 3}
    if !in arr {
        println("test")
    }
}
