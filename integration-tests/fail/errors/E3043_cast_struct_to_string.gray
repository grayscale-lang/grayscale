/*
 * Error Test: E3043 - cannot cast struct to string
 * Expected: E3043 "cannot cast" error
 */

const Point struct {
    x i64
    y i64
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    mut s string = cast(p, string)
}
