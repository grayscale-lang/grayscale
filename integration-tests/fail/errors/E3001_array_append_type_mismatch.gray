/*
 * Error Test: E3001 - array-append-type-mismatch
 * Expected: "type mismatch" or "element type"
 */

import @arrays

do main() {
    mut nums [i64] = {1, 2, 3}
    arrays.append(nums, "hello")  // Should fail: string into i64 array
}
