// Test: E2058 - nested struct declaration inside enum
// Expected: "cannot declare a struct or enum"

const Color enum {
    Red
    const Point struct {
        x i64
    }
}

do main() {
    println("should not compile")
}
