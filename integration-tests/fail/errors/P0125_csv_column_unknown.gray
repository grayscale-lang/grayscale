// expect-contains: .gray:
/* Panic Test: P0125 - csv.column with a name not in the header */
import @csv

do main() {
    mut rows [[string]] = csv.parse("name,age\nAda,36")
    println(csv.column(rows, "salary"))
}
