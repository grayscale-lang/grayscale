/*
 * Error Test: E4005 - map-set-type-mismatch
 * Expected: "module 'maps' has no function named 'set'"
 */

import @maps

do main() {
    mut m map[string:i64] = {"a": 1, "b": 2}
    maps.set(m, "c", "wrong")  // Should fail: string value into i64 map
}
