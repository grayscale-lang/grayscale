/*
 * Error Test: E3095 - 'in' on non-collection type
 * Expected: "'in' only works with arrays, maps, and strings; got 'i64'"
 */

do main() {
    mut x i64 = 42
    if 5 in x {
        println("found")
    }
}
