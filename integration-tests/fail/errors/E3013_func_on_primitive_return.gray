/*
 * Error Test: E3013 - function call on non-struct return value
 * Expected: "has no functions" or "has no fields"
 */


do get_num() -> i64 {
    return 42
}

do main() {
    mut x = get_num().something()
}
