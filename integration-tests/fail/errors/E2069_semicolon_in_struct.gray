/*
 * Error Test: E2069 - semicolons not allowed in struct declarations
 * Expected: error rejecting semicolons
 */

const Point struct {
    x i64;
    y i64
}

do main() {
    println("should not compile")
}
