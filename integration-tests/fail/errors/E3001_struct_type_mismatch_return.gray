/*
 * Error Test: E3001 - returning wrong struct type
 * Expected: "return type mismatch: expected struct 'Cat', got struct 'Dog'" error
 */

const Cat struct {
    name string
}

const Dog struct {
    name string
}

do get_cat() -> Cat {
    return Dog{name: "Rex"}
}

do main() {
    mut c = get_cat()
}
