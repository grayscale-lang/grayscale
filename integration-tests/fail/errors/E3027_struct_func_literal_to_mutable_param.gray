/*
 * Error Test: E3027 - literal passed to mutable (&) param of struct function
 * Expected: "cannot pass a literal or expression to mutable parameter" error
 */

const Calculator struct {
    _pad i64

    do double(&x i64) {
        x = x * 2
    }
}

do main() {
    Calculator.double(42)
}
