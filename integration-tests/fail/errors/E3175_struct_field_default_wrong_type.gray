/*
 * Error Test: E3175 - struct field default value has wrong type
 * Expected: E3175 error
 */
// expect-error: E3175
// expect-error-count: 1

const S struct {
    x i64 = "not an i64"
}

do main() {
    mut s S = S{}
    println("${s.x}")
}
