/*
 * Error Test: E5026 - channels.send passed string instead of i64
 * Expected: E5026 type mismatch error
 */

import @channels

do main() {
    mut ch = channels.open(10)
    channels.send(ch, "hello")
}
