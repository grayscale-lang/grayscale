/*
 * Error Test: E3001 - struct literal sets pointer field to wrong struct pointer type
 * Expected: type mismatch error — ^Other cannot initialize a ^Node field
 */

const Node struct {
    value i64
    next ^Node
}

const Other struct {
    x i64
}

do main() {
    mut o = new(Other)
    mut a Node = Node{value: 1, next: o}
    println(a.value)
}
