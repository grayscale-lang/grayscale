/*
 * Error Test: E2002 - for x in used with non-range expression
 * Expected: error directing to use for_each instead
 */

do main() {
    mut arr [i64, 3] = {1, 2, 3}
    for x in arr {
        println("${x}")
    }
}
