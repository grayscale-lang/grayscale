/*
 * Error Test: E3006 - too few variables for multi-return destructuring
 * Expected: error about missing variables in destructuring
 */

do get_three() -> (i64, i64, i64) {
    return 1, 2, 3
}

do main() {
    mut a, mut b = get_three()
}
