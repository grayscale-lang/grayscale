/* Panic Test: P0034 - cannot modify array during for_each */
import @arrays

do main() {
    mut arr [int] = {1, 2, 3, 4, 5}
    for_each item in arr {
        arrays.append(&arr, item * 2)
    }
    println(arr)
}
