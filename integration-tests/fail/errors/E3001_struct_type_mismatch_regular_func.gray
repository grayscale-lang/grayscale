/*
 * Error Test: E3001 - passing wrong struct type to a regular function
 * Expected: "expected struct 'Point', got struct 'Color'"
 */

const Point struct {
    x i64
    y i64
}

const Color struct {
    r i64
    g i64
}

do process(p Point) -> i64 {
    return p.x
}

do main() {
    mut c = Color{r: 255, g: 0}
    mut n i64 = process(c)
}
