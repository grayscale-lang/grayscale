/*
 * Error Test: E3013 - ref() rejects a dynamic array index expression;
 * the backing store relocates when the array grows.
 */
// expect-error: E3013
// expect-error-count: 1

do main() {
    mut a [int] = {10, 20, 30, 40}
    mut r = ref(a[1])
}
