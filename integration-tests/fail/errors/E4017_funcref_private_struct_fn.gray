/*
 * Error Test: E4017 - func reference to private struct function from outside
 * Expected: E4017 error, compilation fails
 */

const Calc struct {
    value i64

    private do secret(n i64) -> i64 {
        return n * 99
    }
}

do main() {
    const f = ()Calc.secret
    println(f(2))
}
