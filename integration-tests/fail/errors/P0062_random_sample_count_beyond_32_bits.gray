// expect-contains: .gray:
/* Panic Test: P0062 - random.sample with a count that only fits in 32 bits once truncated */
import @random

do main() {
    mut a [int] = {1, 2, 3, 4}
    mut count int = 4294967298
    println(len(random.sample(a, count)))
}
