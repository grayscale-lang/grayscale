/*
 * Error Test: E10006 - stack overflow on recursive return
 * Expected: "stack overflow" or "maximum recursion"
 */


do recurse(n i64) -> i64 {
    return recurse(n + 1)
}

do main() {
    mut x i64 = recurse(0)
    println(x)
}
