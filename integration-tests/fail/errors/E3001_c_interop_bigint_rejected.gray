/*
 * Error Test: E3001 - bigint types cannot be passed to C functions
 * Expected: error about i128 at C boundary
 */

extern import "stdio.h"

do main() {
    mut x i128 = 42
    extern.printf("%d", x)
}
