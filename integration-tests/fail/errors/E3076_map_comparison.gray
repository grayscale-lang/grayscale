/*
 * Error Test: E3076 - maps cannot be compared
 * Expected: "maps cannot be compared with comparison operators"
 */

do main() {
    mut m1 map[string:int] = {"a": 1}
    mut m2 map[string:int] = {"a": 1}
    if m1 == m2 {
        println("equal")
    }
}
