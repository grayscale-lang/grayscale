/*
 * Error Test: integer-overflow (i64 += overflow)
 * Expected: "addition result is too large"
 * Compound += on plain i64 must overflow-check, not silently wrap.
 */

do main() {
    mut x i64 = 9223372036854775807  // INT64_MAX
    x += 1
}
