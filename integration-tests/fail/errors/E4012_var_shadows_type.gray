/*
 * Error Test: E4012 - variable shadows a type definition
 * Expected: "variable 'Point' shadows a type definition with the same name"
 */

const Point struct {
    x i64
    y i64
}

do main() {
    mut Point i64 = 5
}
