const Shape enum {
    Circle(f64)
    Rect(f64, f64)
    Point
}

do main() {
    mut s1 Shape = Shape.Circle(3.14)
    mut s2 Shape = Shape.Circle(3.14)
    if s1 == s2 {
        println("equal")
    }
}
