/* Error Test: E2088 - 'when' crossed with 'case'
 *
 * when/is and switch/case are tracked jointly: both words of the pair
 * must come from the same dialect.
 */
// expect-error: E2088
do main() {
    mut x int = 1
    when x {
        case 1 { println("one") }
        default { println("other") }
    }
}
