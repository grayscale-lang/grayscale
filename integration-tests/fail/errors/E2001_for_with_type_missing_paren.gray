/*
 * Error Test: E2001 - for loop with type annotation and missing closing paren
 * Expected: "expected ), got { instead"
 */

do main() {
    for (i i64 in range(0, 3) {
        mut x i64 = i
    }
}
