/*
 * Error Test: E5008 - struct function called with too many arguments
 * Expected: E5008 "function 'Math.zero' expects 0 argument(s), got 2"
 * Covers: struct function arg count validation
 */

const Math struct {
    _pad i64

    do zero() -> i64 {
        return 0
    }
}

do main() {
    mut r i64 = Math.zero(10, 20)
    println("${r}")
}
