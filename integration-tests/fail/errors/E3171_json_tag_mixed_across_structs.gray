// expect-error: E3171
// expect-error-count: 1
// Two #json structs in the same file: one tagged, one untagged.

import @json

#json
const A struct {
    name string `json:"Name"`
}

#json
const B struct {
    age int
}

do main() {}
