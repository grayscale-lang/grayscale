/*
 * Error Test: E3027 - const passed to mutable (&) param of struct function
 * Expected: "cannot pass constant to mutable parameter" error
 */

const Util struct {
    _pad i64

    do double(&x i64) {
        x = x * 2
    }
}

do main() {
    const val i64 = 5
    Util.double(val)
}
