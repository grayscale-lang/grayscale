/*
 * Error Test: E3027 - literal passed to mutable (&) param via function reference
 * Expected: "cannot pass a literal or expression to mutable parameter" error
 */

do modify(&x i64) {
    x = 99
}

do main() {
    mut fn_ref func(&i64) = ()modify
    fn_ref(42)
}
