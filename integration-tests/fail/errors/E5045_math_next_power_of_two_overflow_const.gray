/*
 * Error Test: E5045 - constant argument outside a stdlib function's domain
 * math.next_power_of_two of a constant whose next power of two overflows int
 * (runtime P0106).
 *
 * expect-error: E5045
 */

import @math

do main() {
    println(math.next_power_of_two(9000000000000000000))
}
