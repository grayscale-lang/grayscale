/*
 * Error Test: E3001 - assigning wrong struct pointer type to a pointer field
 * Expected: type mismatch error — ^Other cannot be assigned to ^Node field
 */

const Node struct {
    value i64
    next ^Node
}

const Other struct {
    x i64
}

do main() {
    mut a = new(Node)
    mut o = new(Other)
    a.next = o
}
