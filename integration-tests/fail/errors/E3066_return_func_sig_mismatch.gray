/*
 * Error Test: E3066 - returning a func ref with mismatched signature
 * Expected: compile-time error on the return statement
 */

do takes_float(n f64) -> i64 { return 0 }

do get_fn() -> func(i64) -> i64 {
    return ()takes_float
}

do main() {
    const f = get_fn()
    println(f(5))
}
