/*
 * Error Test: E2002 - invalid-enum-value
 * Expected: "enum value" or "reserved"
 */

enum Status {
    i64,     // i64 is a reserved keyword
    ACTIVE
}

do main() {
    mut s = Status.ACTIVE
}
