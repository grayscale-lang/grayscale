/*
 * Error Test: E3001 - ref() calls validate argument types
 * Expected: "expected i64, got string"
 */

do add(a i64, b i64) -> i64 {
    return a + b
}

do main() {
    mut r = ref(add)
    println(r("hello", 1))
}
