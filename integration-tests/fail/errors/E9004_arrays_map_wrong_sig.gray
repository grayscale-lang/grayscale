/*
 * Error Test: E9004 - arrays.map() callback signature mismatch
 * Expected: E9004 error (map callback must take 1 parameter)
 */

import @arrays

do bad_map(a i64, b i64) -> i64 { return a + b }

do main() {
    mut nums [i64] = {1, 2, 3}
    mut result [i64] = arrays.map(nums, ()bad_map)
}
