/*
 * Error Test: E2002 - invalid-struct-field
 * Expected: "struct field" or "reserved"
 */

struct Person {
    i64 string  // i64 is a reserved keyword
}

do main() {
    mut p Person = new Person
}
