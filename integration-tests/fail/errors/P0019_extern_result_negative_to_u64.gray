/*
 * Panic Test: P0019 - a negative C interop result assigned to u64/u64 must
 * panic rather than silently reinterpreting as a huge unsigned number.
 */

extern import "stdlib.h"

do main() {
    mut u u64 = extern.atoi("-1")
    println(u)
}
