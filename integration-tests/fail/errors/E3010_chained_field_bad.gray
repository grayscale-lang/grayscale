/* Error Test: E3010 - invalid field on chained struct access */
const Inner struct {
    val i64
}
const Outer struct {
    inner Inner
}
do main() {
    mut o = Outer{inner: Inner{val: 42}}
    mut x i64 = o.inner.nonexistent
    println(x)
}
