/*
 * Error Test: E2068 - struct declared with mut instead of const
 * Expected: "structs must be declared with 'const', not 'mut'" error
 */

mut Point struct {
    x i64
    y i64
}

do main() {
    mut p = new(Point)
}
