// expect-error: E3156
// expect-error-count: 1
do main() {
    mut x i64 = 1
    mut s string = "hi"
    mut p ^i64 = addr(x)
    mut q ^string = addr(s)
    if p == q {
        println("equal")
    }
}
