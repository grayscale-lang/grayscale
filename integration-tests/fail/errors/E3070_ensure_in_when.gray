/*
 * Error Test: E3070 - ensure-not-top-level (in when case)
 * Expected: "'ensure' may only appear at the top level"
 */

do cleanup() { println("cleanup") }

do main() {
    mut x i64 = 1
    when x {
        is 1 { ensure cleanup() }
        default { }
    }
}
