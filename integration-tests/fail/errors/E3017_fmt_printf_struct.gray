/*
 * Error Test: E3017 - fmt.printf cannot format struct
 * Expected: "cannot format value of type"
 */

import @fmt

const Point struct {
    x i64
    y i64
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    fmt.printf("%s", p)
}
