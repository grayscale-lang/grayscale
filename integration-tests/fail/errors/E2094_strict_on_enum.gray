/*
 * Error Test: E2094 - #strict on an enum declaration instead of a when statement
 */

// expect-error: E2094
// expect-error-count: 1

#strict
const Color enum {
    RED
    GREEN
}

do main() {
    mut c Color = Color.RED
    println("${c}")
}
