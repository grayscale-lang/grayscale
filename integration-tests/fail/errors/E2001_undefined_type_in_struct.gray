/*
 * Error Test: E2001 - unexpected symbol from an undefined type in a struct
 */

// expect-error: E2001

def Person struct {
    name string
    data UnknownType  // Unknown type in struct field
}

do main() {
    mut p Person = new Person{}
}
