/*
 * Error Test: E3098 - struct type mismatch through pointer dereference
 * ptrA^ = ptrB^ where ptrA and ptrB point to
 * different struct types was silently allowed by the typechecker.
 * Expected: E3098
 */

const Vec2 struct {
    x i64
    y i64
}

const Vec3 struct {
    x i64
    y i64
    z i64
}

do main() {
    mut v2 = new(Vec2)
    mut v3 = new(Vec3)
    v3^ = v2^
}
