/*
 * Error Test: E2002 - undefined-struct-field
 * Expected: "struct field" or "not found"
 */

struct Person {
    name string,
    age i64
}

do main() {
    mut p Person = new Person
    p.height = 180  // height field does not exist
}
