/*
 * Error Test: E5013 - function call in file-scope initializer
 * Expected: "function calls are not allowed in file-scope initializers"
 */

do compute() -> i64 {
    return 42
}

mut result i64 = compute()

do main() {
    println("${result}")
}
