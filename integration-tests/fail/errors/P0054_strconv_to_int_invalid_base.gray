/* Panic Test: P0054 - strconv.to_int with invalid base at runtime */
import @strconv

do get_base() -> int {
    return 99
}

do main() {
    mut base int = get_base()
    mut val, mut err = strconv.to_int("42", base)
    println(val)
}
