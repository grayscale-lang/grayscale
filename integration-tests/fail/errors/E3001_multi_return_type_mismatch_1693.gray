/*
 * Error Test: E3001 - multi-return-type-mismatch
 * Expected: "type mismatch" or "multi-return"
 */

do get_pair() -> (i64, string) {
    return 42, "hello"
}

do main() {
    // Types reversed - should fail
    mut x string, y i64 = get_pair()
}
