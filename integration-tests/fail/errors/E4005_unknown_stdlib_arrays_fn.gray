/*
 * Error Test: E4005 - typo'd call on the @arrays stdlib module
 *
 * same bug class as the @strings case,
 * different module.
 */

import @arrays

do main() {
    mut a [i64] = {1, 2, 3}
    arrays.bogus(a)
}
