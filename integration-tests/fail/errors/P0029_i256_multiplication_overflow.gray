/* Panic Test: P0029 - i256 multiplication overflow */
do main() {
    mut x = i256(9223372036854775807)
    mut big = x * x
    big = big * big
    big = big * big
    mut result = big * big
    println(result)
}
