/*
 * Error Test: E3001 - cannot modify field of immutable struct created with new()
 * Expected: compiler should reject this with E3001
 */

const Person struct {
    name string
    age i64
}

do main() {
    const p Person = new(Person)
    p.name = "Alice"  // ERROR: cannot modify field of const struct
}
