/*
 * Error Test: E2002 - inline struct with semicolons
 * Expected: error rejecting semicolons in struct body
 */

const Point struct { x i64; y i64 }

do main() {
    println("should not compile")
}
