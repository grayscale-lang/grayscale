/*
 * Panic Test: P0019 - a negative C interop result assigned to uint/u64 must
 * panic rather than silently reinterpreting as a huge unsigned number.
 */

extern import "stdlib.h"

do main() {
    mut u uint = extern.atoi("-1")
    println(u)
}
