/* Error Test: E3001 - passing [i64] to function expecting [string] */
do join(arr [string]) -> string {
    mut result string = ""
    for_each s in arr { result = "${result}${s}" }
    return result
}
do main() {
    mut a [i64] = {1, 2, 3}
    println(join(a))
}
