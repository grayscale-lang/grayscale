/*
 * Error Test: E2002 - enum-mixed-types
 * Expected: "mixed type" or "same type"
 */

enum Status [string] {
    ACTIVE = "active",
    PENDING = 1  // mixing string and i64
}

do main() {
    mut s = Status.ACTIVE
}
