/*
 * Error Test: E3100 - a struct type name passed to a builtin
 * Expected: "type name 'Point' cannot be used as a value"
 */

// expect-error: E3100
// expect-error-count: 1

const Point struct {
    x int
    y int
}

do main() {
    println(Point)
}
