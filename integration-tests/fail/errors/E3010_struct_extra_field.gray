/*
 * Error Test: E3010 - struct literal has extra field
 * Expected: compiler should reject this with E3010
 */

const Person struct {
    name string
    age i64
}

do main() {
    mut p = Person{name: "Alice", age: 30, email: "test@test.com"}
}
