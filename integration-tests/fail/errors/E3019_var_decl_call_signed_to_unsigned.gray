// Error Test: E3019 - a mut unsigned declaration initialized by an
// i64-returning call reinterprets the sign bit and must be rejected.
// expect-error: E3019
// expect-error-count: 1

do get_neg() -> i64 {
    return -5
}

do main() {
    mut u u64 = get_neg()
    println("u=${u}")
}
