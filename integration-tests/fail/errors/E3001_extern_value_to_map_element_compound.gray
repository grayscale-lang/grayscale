/*
 * Error Test: E3001 - an extern C call result added into a map element with
 * a compound assignment.
 */
// expect-error: E3001
// expect-error-count: 1

extern import "stdlib.h"

do main() {
    mut m map[string:i64] = {"a": 1}
    m["a"] += extern.abs(-3)
    println(m["a"])
}
