/*
 * Error Test: E9004 - arrays.reduce() callback must take 2 parameters
 * Expected: E9004 error
 */

import @arrays

do bad_reduce(x i64) -> i64 { return x }

do main() {
    mut nums [i64] = {1, 2, 3}
    mut result i64 = arrays.reduce(nums, 0, ()bad_reduce)
}
