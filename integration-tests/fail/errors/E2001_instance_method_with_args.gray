/*
 * Error Test: E2001 - struct function with args called on instance
 * Expected: E2001 unexpected symbol (parser rejects instance.func() syntax)
 */

const Point struct {
    x i64
    y i64
}

do Point.add(a Point, b Point) -> Point {
    return Point{x: a.x + b.x, y: a.y + b.y}
}

do main() {
    mut p Point = Point{x: 1, y: 2}
    mut q Point = Point{x: 3, y: 4}
    mut r Point = p.add(p, q)
}
