/*
 * Error Test: E5009 - invalid base for strconv.format_u64
 * Expected: "invalid base" error at compile time
 */

// expect-error: E5009

import @strconv

do main() {
    mut n u64 = 255
    mut s string = strconv.format_u64(n, 99)
    println(s)
}
