/*
 * Error Test: E3013 - chained field access on non-struct type
 * Expected: "has no fields" error
 */

const Point struct {
    x i64
    y i64
}

do main() {
    mut p = Point{x: 1, y: 2}
    mut val = p.x.z
}
