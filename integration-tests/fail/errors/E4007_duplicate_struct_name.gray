/*
 * Error Test: E4007 - duplicate struct name
 * Expected: "a type named 'Point' is already declared" error
 */

const Point struct {
    x i64
    y i64
}

const Point struct {
    a f64
    b f64
}

do main() {
    mut p = new(Point)
}
