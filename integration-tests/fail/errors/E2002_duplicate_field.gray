/*
 * Error Test: E2002 - duplicate-field (uses old 'struct' syntax)
 * Expected: E2002 unexpected symbol
 */

struct Person {
    name string,
    age i64,
    name string  // duplicate field
}

do main() {
    mut p Person = new Person
}
