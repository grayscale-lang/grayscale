/*
 * Error Test: E5026 - 'arrays.rotate's shift-count argument must be an i64.
 * A non-i64 value previously compiled straight through the typechecker and
 * failed only at the C-compilation stage, leaking a raw clang diagnostic.
 */
// expect-error: E5026
// expect-error-count: 1

import @arrays

do main() {
    mut arr [i64] = {1, 2, 3}
    mut r [i64] = arrays.rotate(arr, "two")
    println(r)
}
