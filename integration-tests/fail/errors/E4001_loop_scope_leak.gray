/*
 * Error Test: E4001 - loop-scope-leak
 * Expected: "undefined variable 'inner'"
 */

do main() {
    mut count i64 = 0
    loop {
        mut inner i64 = 99
        count++
        if count >= 1 {
            break
        }
    }
    println("${inner}")
}
