/*
 * Error Test: E3001 - struct field assignment type mismatch
 * Expected: compiler should reject this with E3001
 */

const Person struct {
    name string
    age i64
}

do main() {
    mut p = Person{name: "John", age: 30}
    p.name = 123
}
