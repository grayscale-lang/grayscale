/*
 * Error Test: E2063 - duplicate named return value
 * Expected: "duplicate named return value 'result'" error
 */

do compute(x i64) -> (result i64, result i64) {
    return x, x * 2
}

do main() {
    mut a, b = compute(5)
}
